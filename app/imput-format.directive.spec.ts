import { ImputFormatDirective } from './imput-format.directive';

describe('ImputFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new ImputFormatDirective();
    expect(directive).toBeTruthy();
  });
});
